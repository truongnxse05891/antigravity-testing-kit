# Block Variant Picker - Manual Test Cases
*(Generated via QUICK Mode: Phương pháp kiểm thử Nhanh)*

Dựa vào các Acceptance Criteria, tính năng Block Variant Picker đã được chia thành các nhóm Test Scenarios bao quát toàn bộ Happy paths, Negative paths, và Boundary cases theo 4 nhóm logic:
1. **Editor / Builder Logic** (Thao tác trên Store builder)
2. **Design & Display Logic** (Tương tác style hiển thị)
3. **Storefront Variant Selection** (Click chọn product ngoài Storefront)
4. **Size chart & Custom Option Logic** (Logic phụ thuộc nền tảng)

## Bảng Test Cases Chi Tiết

| TC ID | Module | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `ECOM_VP_TC_001` | Builder Logic | Kiểm tra thao tác Insert block và Auto-map Data Source | Admin web builder đang mở page thiết kế 1 Product cụ thể. | 1. Mở Insert panel ở sidebar. <br> 2. Tìm và kéo block `Variant Picker` vào Content page. <br> 3. Kiểm tra dữ liệu block load. | Product Source: `Ao thun Unisex T1` có Option `Color` và `Size`. | Block được chèn ở vị trí kéo thả. Tự động parse và map ra UI hiển thị tùy chọn `Color` và `Size` theo đúng single source data. | Critical |
| `ECOM_VP_TC_002` | Builder Logic | Kiểm tra thứ tự Option trùng khớp Admin | Sản phẩm có sắp xếp thứ tự các options. | 1. Chèn block Variant Picker. <br> 2. So sánh thứ tự các title và list value hiển thị đối chiếu Admin. | Admin đang sếp: 1-Material, 2-Style, 3-Size. | Variant Picker hiển thị options từ trên xuống dưới tuân thủ đúng thứ tự 1-Material, 2-Style, 3-Size. | High |
| `ECOM_VP_TC_003` | Builder Logic | Ràng buộc Min-width khi resize | Variant Picker đang nằm ở Editor. | 1. Trỏ chuột vào viền block Variant Picker. <br> 2. Kéo thu nhỏ width hết mưc có thể. | Min-width default của block (VD: 150px). | Block không thể thu bé quá min-width hệ thống chặn. | Medium |
| `ECOM_VP_TC_004` | Builder Logic | Kiểm tra chiều cao Height "Auto hug content" | Có 1 block Variant Picker đang hiển thị Layout dạng Button | 1. Cấu hình Option Style có 10 Button values nằm trên 3 hàng. <br> 2. Chuyển sang Dropdown (còn 1 hàng). <br> 3. Kiểm tra chiều cao viền Block. | 10 values: `S, M, L, XL, 2XL, 3XL, 4XL, 5XL...` | Block tự động tăng chiều cao (height) khi có nhiều hàng và ôm khít (co ngắn lại) khi layout chỉ còn 1 hàng Dropdown. | Medium |
| `ECOM_VP_TC_005` | Design Setting | Edit Layout Button vs Dropdown | Đang ở tab setting Design của Block đó. | 1. Đổi 'Layout' setting từ `Button` thành `Dropdown`. <br> 2. Đổi Spacing between options và variants bằng thanh trượt. | Kéo spacing option = `10px`, variant = `5px`. | 1. Layout bên ngoài đổi giao diện lập tức theo dạng select dropdown list. <br> 2. Khoảng cách margin/padding nới lỏng đúng thông số nhập. | High |
| `ECOM_VP_TC_006` | Design Setting | Mức độ ưu tiên Color preview vs Image preview | Product setup sẵn cả option Color code và File Image cho Variant. | 1. Tại Design tab, bật đồng thời `Color preview` và `Image preview`. <br> 2. Quan sát storefront hiện gì. | Product `Ao thun` có màu `#FF0000` (đỏ) và ảnh `red_shirt.png`. | Color preview (mã màu #FF0000) được load hiển thị. Image không lên (Do rule: Ưu tiên color trước image). | High |
| `ECOM_VP_TC_007` | Storefront Variant | Phân luồng Add To Cart theo availability của Option Cross-pair | Sản phẩm có các Option, nhưng có cặp Option-value **Không tồn tại** trong hệ thống. | 1. Truy cập Storefront với vai trò Buyer. <br> 2. Chọn cặp: Màu "Đỏ" + Kích cỡ "5XL". <br> 3. Click nút Mua. | Hệ thống Admin báo: Đỏ và 5XL không mix chung thành 1 ID Variant bán. | Hai nút `Add to cart` và `Buy Now` lập tức chuyển trạng thái xám (unavailable / Disabled). Không cho thanh toán. | Critical |
| `ECOM_VP_TC_008` | Storefront Variant | Variant trạng thái Sold Out | Admin set 1 Variant = Inventory `0`. | 1. Vào Webfront. <br> 2. Chọn đúng Variation đang hêt hàng. | Chọn variant `Blue` + `S` (Inventory = 0) | Cặp đó sẽ báo "Out of stock/Sold out" trên UI và nút Add To Cart bị vô hiệu hóa. | High |
| `ECOM_VP_TC_009` | ShopBase Rules | Setup logic Size Chart trên Shopbase | Cửa hàng thuộc tệp ShopBase (SB) | 1. Tạo sp KHÔNG có option `Size` -> Check Size chart xuất hiện không? <br> 2. Tạo sp có Option `Size` và Bật bảng size chart đúng hướng dẫn. <br> 3. Kiểm tra webfront. | Sp1: Opt `Color`. Sp2: Opt `Size` (Có enable data size_chart_1). | Sp1: Size chart KHÔNG CÓ trên ui. <br> Sp2: Text/Button mở Size Chart xuất hiện cạnh option `Size`. | High |
| `ECOM_VP_TC_010` | PrintBase Rules | Setup Logic Size Chart trên PrintBase | Cửa hàng nền tảng PrintBase (PB) | 1. Enable Data Hive-pbase cho Size chart (Không có Option Size cụ thể). <br> 2. Cấu hình PB config show. <br> 3. Mở Size chart click thử tính năng Inch <-> CM. | Bật Config, vào sp xem. Action toggle đo lường từ `Inch` sang `cm`. | 1. Bảng Size chart luôn show do config PB enable. <br> 2. Toggle Inch/CM thực hiện convert logic số học thành công. | High |
| `ECOM_VP_TC_011` | PlusBase Rules | Setup logic Size Chart trên PlusBase | Cửa hàng PlusBase (PLB) | 1. Thiết lập Shop Template config với 1 Option ID bất kỳ khác chữ Size (vd: Kíchthước). <br> 2. Bật Config hiển thị. | Option `KíchThước` có bật toggle. | Dữ liệu kéo từ PLB template. Cứ có config là biểu tượng Size chart hiển thị (không hardcode bắt buộc Option name = Size). Convert inch>cm mượt. | High |
| `ECOM_VP_TC_012` | Custom Option | Logic hiển thị nút "Preview your design" | Store Print-on-Demand (POD) | 1. Bật option "Preview button" ở Setting POD. <br> 2. Sản phẩm có setup "Custom option" (Nhập text cá nhân hóa). <br> 3. Check ui Variant picker. | Custom txt: `"Tên bạn in lên áo"`. On flag Dashboard > Settings. | Dưới các options sẽ đính kèm button `"Preview your design"` dành riêng cho Personalized design. | Medium |
| `ECOM_VP_TC_013` | Custom Option | Logic Image Crop PB/Printhub vs Shopbase (Exception path) | 2 Cửa hàng: 1 cái PB/Printhub, 1 cái SB thuần. | 1. Upload file cho Custom option qua nền tảng Printbase/Printhub. <br> 2. Cùng thao tác upload file tại ShopBase thuần. | File ảnh up lên: `avatar-custom.png`. Cùng 1 form upload. | 1. Tại PB/hub: Mở Popup yêu cầu người dùng CROP ẢNH (do phải tuân thủ in ấn Ratio). <br> 2. Tại SB: Tải ảnh thẳng gốc lên, không có Popup crop. | Critical |
